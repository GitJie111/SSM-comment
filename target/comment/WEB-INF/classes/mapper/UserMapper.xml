<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xunqi.mapper.UserMapper">


    <select id="login" resultType="org.xunqi.pojo.User" parameterType="string">
        select id,name,password,ch_name,group_id from sys_user where name = #{name}
    </select>
</mapper>